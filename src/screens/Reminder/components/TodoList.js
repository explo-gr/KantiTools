const note = {
    title: null,
    description: null,
    tint: null,
    id: null,
}

const TodoItem = (title, description, tint) => {
    return (
        <></>
    )
}

const TodoList = () => {

    const notes = [1, 2, 3];

    return (
        <View>
            {}
        </View>
    )
}

//https://react.dev/learn/rendering-lists#where-to-get-your-key